Hi,

These folder contains the Kubernetes deployment ".yml" files.

which can be used to deploy the mentioned Applications.

The Image used to create container inside the pod is already mentioned in the ".yml" files itself.
